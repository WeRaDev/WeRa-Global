# kb-problem

## Purpose
Canonical domain folder inside `KnowledgeBase/` for single-repository KB architecture.

## Authoring policy
- Canonical authoring happens in this folder tree.
- Changes must include metadata/provenance contract on operational records.
- Governance routing follows `temporal_scope`.
